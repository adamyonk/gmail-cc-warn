{
  "name": "gmail-cc-warn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'test/**/*.test.js'",
    "sync": "rsync -a --delete background/ firefox/background/ && rsync -a --delete content/ firefox/content/ && rsync -a --delete icons/ firefox/icons/ && rsync -a --delete options/ firefox/options/ && rsync -a --delete shared/ firefox/shared/ && cp manifest.json safari-src/manifest.json",
    "firefox:sync": "npm run sync"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
